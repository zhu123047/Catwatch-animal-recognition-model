# Training data (not committed)

Download images into `images/` using:

```bash
python -m epic3_training.download_inaturalist_subset --targets epic3_training/species_targets.json
```

See `epic3_training/download_inaturalist_subset.py` for quotas and size limits.
